# Search-R1 后续实验步骤

## 1. 项目题目

**Search-R1 Lite：低资源场景下单步与多步检索行为分析**

---

## 2. 实验目标

在不优先追求完整 RL 重训练复现的前提下，先复现 Search-R1 的 reasoning + retrieval 推理闭环，并围绕 **单步检索** 与 **多步检索** 做轻量对比实验，回答以下问题：

1. 相比不检索，单步检索是否能提升回答质量？
2. 相比单步检索，多步检索是否真的带来稳定收益？
3. 在 3B 小模型和低资源条件下，多步检索是否会引入更多重复搜索、无效搜索和证据利用不足问题？
4. 是否存在“限制搜索预算反而更稳定”的现象？

---

## 3. 实验范围

本阶段先做 **推理行为分析**，不把重点放在完整 PPO / GRPO 训练复现上。

优先级如下：

### Phase 1：推理闭环复现
- 跑通 retriever server
- 跑通 `infer.py`
- 记录 query、检索结果、最终回答与搜索轮数

### Phase 2：单步 vs 多步检索对比实验
- no-search baseline
- single-step
- multi-step-2
- 可选：multi-step-3

### Phase 3：失败模式分析
- 无效搜索
- 重复搜索
- 检索到证据但未被利用
- 多步收益不明显或引入噪声

### Phase 4：可选增强
如果前面进展顺利，再考虑：
- 小步数 debug 训练
- 简单搜索预算控制规则
- query 重复时提前停止搜索

---

## 4. 推荐实验配置

### 模型建议
优先尝试：
- `Qwen/Qwen2.5-3B`
- 如果资源不够，先用 `Qwen/Qwen2.5-1.5B` 验证流程

### 资源建议
#### 仅做推理行为分析
- 1 x 24GB GPU 基本可行
- 32GB RAM

#### 做小步数 debug 训练
- 建议 2 x 24GB 或以上
- 64GB RAM 更稳

### 当前阶段建议
先完成 **3B 推理闭环 + 单步/多步对比实验**，不要先冲完整 8 卡 PPO 训练脚本。

---

## 5. 具体实施步骤

## Step 1：跑通 baseline 推理流程
目标：确认模型能够完成以下流程：
- 接收问题
- 生成 `<search>...</search>`
- 请求检索接口
- 注入 evidence
- 继续推理
- 输出最终 answer

建议操作：
1. 启动 retriever server
2. 运行 `infer.py`
3. 保存 10~20 条原始样例日志

本步产物：
- 可运行的推理闭环
- 原始日志样例

---

## Step 2：为推理脚本加入搜索预算控制
目标：构造单步与多步检索实验设置。

建议增加参数：
- `max_search_turns = 0`：no-search
- `max_search_turns = 1`：single-step
- `max_search_turns = 2`：multi-step-2
- `max_search_turns = 3`：可选扩展

实现思路：
- 每次执行一次 `<search>` 时计数加一
- 达到上限后不再继续调用 search
- 让模型基于已有上下文直接结束回答

本步产物：
- 三组及以上可控实验模式

---

## Step 3：准备小规模评测集
建议规模：
- 50~100 条问题

建议构成：
- 30~50 条单跳事实问题
- 20~50 条更依赖外部证据或多步信息的问题

要求：
- 三组实验使用同一批问题
- 固定温度和随机设置，尽量减少采样噪声
- 每道题保存原始 query 和 evidence

本步产物：
- 小规模固定评测集

---

## Step 4：定义实验指标
至少保留以下四类指标：

### 1. 最终回答质量
建议人工标注：
- 2：正确
- 1：部分正确
- 0：错误

### 2. 平均搜索次数
统计每种模式下平均 search 次数。

### 3. 重复搜索率
判断是否出现：
- 重复 query
- 高度相似 query
- 重复 evidence

### 4. 证据利用情况
判断：
- 检索结果是否相关
- 检索到的证据是否真的进入最终回答

可选指标：
- 无效搜索率
- 每题平均轨迹长度
- 多步检索收益出现在哪类题型上

---

## Step 5：做失败模式分类
建议使用以下标签：

- `invalid_search`：query 明显不相关或过于发散
- `repeated_search`：多次 query 高度重复
- `evidence_not_used`：检索到正确信息但回答未利用
- `wrong_reasoning`：证据相关但推理链错误
- `hallucination`：最终回答脱离证据
- `none`：无明显失败

目标：
不仅看分数，还要分析 **Search-R1 为什么失效**。

---

## Step 6：最小实验矩阵
建议优先做：

| 模式 | 含义 |
|---|---|
| no-search | 不允许搜索 |
| single-step | 最多搜索 1 次 |
| multi-step-2 | 最多搜索 2 次 |

可选补充：

| 模式 | 含义 |
|---|---|
| multi-step-3 | 最多搜索 3 次 |

实验重点问题：
1. single-step 是否优于 no-search？
2. multi-step-2 是否显著优于 single-step？
3. 多步搜索收益是否主要出现在复杂问题中？
4. 多步搜索是否带来更多无效/重复搜索？

---

## 6. 实验记录模板

建议用表格记录：

| sample_id | question | difficulty | mode | answer_score | search_count | duplicate_query | useful_evidence | evidence_used_in_answer | failure_type | notes |
|---|---|---|---|---:|---:|---|---:|---|---|---|

字段建议：
- `difficulty`：single-hop / multi-hop
- `mode`：no-search / single-step / multi-step-2
- `answer_score`：0 / 1 / 2
- `duplicate_query`：yes / no
- `useful_evidence`：0 / 1 / 2
- `failure_type`：见上面的失败标签

---

## 7. 建议的一周推进节奏

### Day 1
- 跑通 retriever + infer
- 保存 10 条样例日志

### Day 2
- 加入搜索预算控制
- 跑通 no-search / single-step / multi-step-2

### Day 3
- 准备 50~100 条测试问题
- 开始批量跑实验

### Day 4
- 完成人工标注
- 统计回答质量、平均搜索次数、重复搜索率

### Day 5
- 做失败模式分类
- 整理 3~5 个代表性案例

### Day 6
- 写阶段性结果总结
- 形成简历 bullet 与 2 分钟讲稿

### Day 7
- 若有余力，加入简单提前停止规则
- 否则开始投递并准备面试表达

---

## 8. 最终希望形成的结论

理想情况下，本项目最终可以支持以下类型的结论：

1. 相比 no-search，single-step 检索能够提升部分外部知识型问题的回答质量。
2. multi-step 检索并不一定稳定优于 single-step，其收益受 query 质量和证据利用能力影响较大。
3. 在低资源和小模型场景下，限制搜索预算有时比无约束多步搜索更稳定。
4. Search-R1 的性能瓶颈不仅在模型本身，也体现在工具调用策略和检索轨迹质量上。

---

## 9. 简历描述参考

**Search-R1 Lite：低资源场景下单步与多步检索行为分析（进行中）**
- 复现 Search-R1 中 reasoning + retrieval 的基础交互流程，梳理 policy model、retriever、rollout 与 tool-call 的整体调用链路，完成搜索增强推理闭环验证。
- 在 3B 小模型与低资源配置下设计单步检索、多步检索与无检索对比实验，从回答质量、平均搜索次数、重复 query 比例与证据利用情况等角度分析多轮搜索的实际收益。
- 基于样例级轨迹记录整理失败模式，重点关注无效搜索、重复搜索、检索到证据但未被利用等问题，分析搜索预算与推理稳定性之间的关系。

---

## 10. 当前执行原则

1. 先做小而真实的实验，不追求一开始就复现完整训练结果。
2. 先把推理行为分析做扎实，再考虑 debug 训练。
3. 先产出可讲的对比实验和案例分析，再决定是否加轻量改进。
4. 项目目标是形成 **可写简历、可面试讲清、可继续扩展** 的算法实验材料。

# Reports

本目录只保存来自真实实验的小型结果摘要。

可以提交：

- 实验 summary；
- 小型 metrics JSON；
- 抽样 trajectory；
- 图表；
- failure case 分析；
- environment lock 记录。

不要提交：

- checkpoint；
- 模型权重；
- 数据集；
- 检索 index；
- wandb 原始目录；
- 全量 trajectory dump；
- 大型日志。

所有 README、报告和简历数字必须来自真实日志，不允许编造。
